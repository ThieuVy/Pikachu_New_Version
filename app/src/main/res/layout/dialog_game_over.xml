<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="32dp"
    android:padding="24dp"
    android:background="@drawable/dialog_background">

    <!-- Icon ở trên cùng -->
    <ImageView
        android:id="@+id/iv_dialog_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/img_17"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <!-- Tiêu đề -->
    <TextView
        android:id="@+id/tv_dialog_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Hết Giờ!"
        android:textColor="@android:color/black"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_dialog_icon"
        tools:ignore="HardcodedText" />

    <!-- Nội dung -->
    <TextView
        android:id="@+id/tv_dialog_message"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Bạn đã không hoàn thành màn chơi. Bạn có muốn chơi lại không?"
        android:textColor="@android:color/darker_gray"
        android:textSize="16sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_dialog_title"
        tools:ignore="HardcodedText" />

    <!-- Nút Thoát Game -->
    <Button
        android:id="@+id/btn_dialog_exit"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="8dp"
        android:text="Thoát Game"
        android:textColor="@android:color/black"
        android:background="@drawable/button_outline_background"
        app:layout_constraintEnd_toStartOf="@+id/btn_dialog_replay"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_dialog_message"
        tools:ignore="HardcodedText" />

    <!-- Nút Chơi Lại -->
    <Button
        android:id="@+id/btn_dialog_replay"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:text="Chơi Lại"
        android:textColor="@android:color/white"
        android:background="@drawable/button_solid_background"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toEndOf="@+id/btn_dialog_exit"
        app:layout_constraintTop_toTopOf="@+id/btn_dialog_exit"
        tools:ignore="HardcodedText" />

</androidx.constraintlayout.widget.ConstraintLayout>