<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.App1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.App1" parent="Base.Theme.App1" />

    <!-- ================================================================ -->
    <!--          THÊM STYLE MỚI CỦA BẠN VÀO ĐÂY                           -->
    <!-- ================================================================ -->
    <style name="Theme.App1.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Tùy chỉnh thêm nếu cần, ví dụ màu thanh trạng thái -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m" tools:ignore="ObsoleteSdkInt">false</item>
    </style>

</resources>